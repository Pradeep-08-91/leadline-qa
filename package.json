{
  "name": "leadline-qa",
  "version": "1.0.0",
  "private": true,
  "description": "Automated QA suite for the Leadline demo app",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1"
  }
}
